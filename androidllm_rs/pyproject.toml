[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "androidllm-rs"
version = "0.1.0"
description = "PyO3 accelerator core for androidllm"
requires-python = ">=3.9"

[tool.maturin]
module-name = "androidllm_rs"
